const NotFoundPage: React.FC = () => {
  return (
    <div className="mt-[100px] text-[red] text-center">
      <b className=" text-[64px] ">404</b>
      <h1 className="mt-[50px] text-[24px] font-semibold  ">
        Sorry, we couldn't find that page{' '}
      </h1>
    </div>
  );
};

export default NotFoundPage;
